# 🚀 Быстрый старт деплоя

## ✅ Шаг 1: Добавь GitHub Secrets

Открой: https://github.com/YevgeniyAbramov/sabina-school/settings/secrets/actions

Добавь эти 9 secrets (кнопка "New repository secret"):

| Name | Value |
|------|-------|
| `DOCKERHUB_USERNAME` | `yabramov228` |
| `DOCKERHUB_TOKEN` | `(твой Docker Hub токен)` |
| `VPS_HOST` | `46.101.212.67` |
| `VPS_USERNAME` | `root` |
| `VPS_PORT` | `22` |
| `VPS_SSH_KEY` | см. инструкцию ниже ⬇️ |
| `DB_USER` | `admin` |
| `DB_PASSWORD` | `Aspirine1` |
| `DB_NAME` | `sabina_school` |

---

## 🔑 Получение SSH ключа (VPS_SSH_KEY)

**На своем Mac выполни:**

```bash
# 1. Создай SSH ключ (если еще нет)
ssh-keygen -t ed25519 -C "github-actions"
# Нажми Enter на все вопросы (без пароля!)

# 2. Скопируй публичный ключ на сервер
ssh-copy-id -i ~/.ssh/id_ed25519.pub root@46.101.212.67
# Введи пароль от сервера

# 3. Проверь подключение без пароля
ssh root@46.101.212.67
# Должно подключиться БЕЗ запроса пароля!

# 4. Выйди с сервера
exit

# 5. Выведи приватный ключ
cat ~/.ssh/id_ed25519
```

**Скопируй ВЕСЬ вывод** последней команды (от `-----BEGIN` до `-----END`) и добавь как GitHub Secret `VPS_SSH_KEY`.

---

## 🖥️ Шаг 2: Настрой сервер (один раз)

**Подключись к серверу:**

```bash
ssh root@46.101.212.67
```

**Выполни на сервере:**

```bash
# Скачай и запусти скрипт настройки
curl -o setup.sh https://raw.githubusercontent.com/YevgeniyAbramov/sabina-school/main/deploy/setup-server.sh
chmod +x setup.sh
./setup.sh
```

Скрипт установит Docker, Docker Compose и подготовит все необходимое.

**После установки:**

```bash
cd /opt/sabina-school

# Проверь .env файл (пароли должны совпадать с GitHub Secrets)
cat .env

# Если нужно, отредактируй:
nano .env
```

---

## 🚀 Шаг 3: Первый деплой!

**На своем Mac:**

```bash
cd /Users/abramov/Documents/sabina-sckool

# Добавь все файлы
git add .

# Закоммить
git commit -m "Setup GitHub Actions CI/CD"

# Запуш в GitHub (это запустит автодеплой!)
git push origin main
```

**Проверь деплой:**

Открой: https://github.com/YevgeniyAbramov/sabina-school/actions

Увидишь процесс сборки и деплоя в реальном времени! ⏱️

---

## 🎉 Шаг 4: Инициализация базы данных

**После успешного деплоя, на сервере:**

```bash
ssh root@46.101.212.67
cd /opt/sabina-school

# Запусти инициализацию БД (создаст таблицы и тестового преподавателя)
./init-db.sh

# Проверь статус контейнеров
docker-compose ps

# Посмотри логи
docker-compose logs -f app
```

---

## ✅ Шаг 5: Проверка работы!

Открой в браузере: **http://46.101.212.67:3000**

**Тестовый вход:**
- Логин: `test`
- Пароль: `test`

---

## 🔄 Как обновлять приложение?

Просто делай `git push` - деплой будет автоматическим! 🎉

```bash
# Внес изменения в код
git add .
git commit -m "Updated feature"
git push origin main

# GitHub Actions автоматически:
# ✅ Соберет новый образ
# ✅ Загрузит в Docker Hub
# ✅ Обновит на сервере
```

---

## 🆘 Если что-то пошло не так

### Проверь логи GitHub Actions
https://github.com/YevgeniyAbramov/sabina-school/actions

### Проверь логи на сервере
```bash
ssh root@46.101.212.67
cd /opt/sabina-school
docker-compose logs -f
```

### Перезапусти вручную
```bash
ssh root@46.101.212.67
cd /opt/sabina-school
docker-compose down
docker-compose pull
docker-compose up -d
```

---

## 📊 Полезные команды на сервере

```bash
# Статус контейнеров
docker-compose ps

# Логи приложения
docker-compose logs -f app

# Логи базы данных
docker-compose logs -f db

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Подключиться к БД
docker exec -it sabina_school_db psql -U admin -d sabina_school

# Бэкап БД
docker exec sabina_school_db pg_dump -U admin sabina_school > backup_$(date +%Y%m%d).sql
```

---

## 🎯 Готово! Теперь у тебя:

✅ Автоматический деплой при каждом push
✅ Приложение доступно по http://46.101.212.67:3000
✅ База данных с данными сохраняется между обновлениями
✅ Логи и мониторинг

**Поздравляю с первым деплоем! 🚀**

